# Infrastructure & Deployment
- **Docker**: Supported via `Dockerfile` for both backend and frontend, plus `docker-compose.yml`.

**Evidence (`README.md`)**:
```markdown
### Database: Supabase
...
### Backend: Hugging Face Spaces
...
### Frontend: Vercel (free)
```
