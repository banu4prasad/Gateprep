## 2026-06-05 - Adding accessibility tags to icon-only buttons
**Learning:** Icon-only buttons without `aria-label` and `title` tags reduce accessibility for screen-readers and hide their function from mouse users.
**Action:** Add `aria-label` and `title` to icon-only buttons as a low-risk, high-reward UX improvement.